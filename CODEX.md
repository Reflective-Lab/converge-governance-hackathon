# Codex

This file is the Codex entrypoint. The canonical documentation is in `kb/`.

## Start Here

1. Read `AGENTS.md`.
2. Read `CLAUDE.md` for the shared project philosophy, stack, rules, and Converge guidance.
3. Read `kb/Home.md` as the index. Follow only the links you need.
4. Run `just focus` at session start.

## Workflow

- Shared repo-state checks: `just focus`, `just sync`, `just status`
- Codex workflow mapping: `kb/Workflow/Working with Codex.md`
- Daily flow: `kb/Workflow/Daily Journey.md`

If a user uses Claude-style workflow names such as `/focus`, `/fix`, or `/checkpoint`, treat them as workflow intents and follow the Codex equivalents in the knowledgebase.

When you learn something worth preserving, update `kb/`.
